package practice;
/*
import org.junit.Assert;
import org.junit.Test;
import org.openqa.selenium.By;
import utilities.BaseTestReport;

public class Extend_reports extends BaseTestReport {

    /*
     ● https://the-internet.herokuapp.com/windows adresine gidin.
     ● Sayfadaki textin "Opening a new window" olduğunu doğrulayın.
     ● Sayfa başlığının (title) "The Internet" olduğunu doğrulayın.
     ● Click Here butonuna basın.
     */
/*
    @Test
    public void Test(){

        extentTest = extentReports.createTest("Test", "açıklama");

        extentTest.info("sayfaya gidiliyor");
        driver.get("https://the-internet.herokuapp.com/windows");

        extentTest.info("opening a new window");
        Assert.assertEquals(driver.findElement(By.tagName("h3")).getText(),"Opening a new window");
        extentTest.info("Title da the Internet var mı");
        Assert.assertTrue(driver.getTitle().contains("The Internet"));

        String firstPageHandle=driver.getWindowHandle();

        extentTest.info("Click Here");
        driver.findElement(By.linkText("Click Here")).click();

        extentTest.pass("Test geçer");

    }
 }
 */

